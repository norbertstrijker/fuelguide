export default function AdSlot({ id }) {
  // Placeholder for future AdSense integration.
  // When AdSense is approved:
  // 1. Add AdSense script tag in app/layout.js
  // 2. Replace this div with actual ad unit code
  return <div id={id} className="empty:hidden" />
}
